import React from "react";

function MediaTextPreview({ text }) {
  return (
    <div className="text-container">
      <p className="media-text">{text}</p>
    </div>
  );
}

export default MediaTextPreview;
